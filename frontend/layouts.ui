pub layout: Enum.Layout;

Rect {
  Layout {
    layout: (layout);
    Pane for n in 6 {
      Rect {
        scaleToParent: 1;
        fill: (Brush.rgb(Math.random(), Math.random(), Math.random()));
        (n)
      }
    }
  }
}